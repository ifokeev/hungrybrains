= form_for(current_user.student.relationships.find_by(company_id: company.id), |
           html: { id: "unfollow_company#{company.id}", method: :delete }, remote: true) do |f|                      |
  = f.submit "Отписаться", class: "btn btn-danger btn-sm btn-company"
