.row.vacancy-row
  .col-xs-2.logo
    = image_tag response.vacancy.company.avatar.url(:thumb)
  .col-xs-8
    .row.title
      %p= vacancy_title(response.vacancy)
    .row.description
      %p= vacancy_description(response.vacancy)
  .col-xs-1.worktype
    .row
      = response.status
