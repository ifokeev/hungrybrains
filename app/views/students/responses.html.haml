.row.vacancies.margin-top20  
  .col-xs-9
    .search.row
      - if @responses.any?
        = student_responses_count(@responses)
      - else
        %strong
          Вы не откликнулись ни на одну вакансию
    = render partial: 'response', collection: @responses
  .col-xs-3
    .blank
